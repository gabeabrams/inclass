/**
 * Returns a new tags object with the tagNameToExclude excluded
 * @param {object} tags - the original tags object
 * @param {string} tagNameToExclude - the tagName to exclude
 */
export default (tags, tagNameToExclude) => {

};
