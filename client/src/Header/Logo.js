import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Logo extends Component {
  render() {
    return (
      <span>
        No logo yet
      </span>
    );
  }
}

Logo.propTypes = {
};

Logo.defaultProps = {
};

export default Logo;
